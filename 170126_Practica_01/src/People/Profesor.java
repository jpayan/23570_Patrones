package People;

import Behaviors.Walkable;

public class Profesor extends Person {
    public Profesor(String name, int age, Walkable walkingBehavior) {
        super(name, age, walkingBehavior);
    }
}