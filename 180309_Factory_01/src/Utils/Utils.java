package Utils;

public class Utils {
    public enum PizzaType {
        CHEESE, HAWAIIAN, MEXICAN
    }

    public enum Thickness {
        THIN, NORMAL, THICK
    }
    public enum Size {
        SMALL, MEDIUM, LARGE
    }
}
